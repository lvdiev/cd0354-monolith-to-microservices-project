version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: lvdinh/reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: lvdinh/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: lvdinh/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: lvdinh/udagram-frontend
